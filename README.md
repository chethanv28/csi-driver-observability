# csi-driver-observability
Prometheus &amp; Grafana observability stack for any Kubernetes CSI driver that exposes Prometheus metrics. Works out of the box with every driver that uses the standard Kubernetes CSI sidecar containers
